export const ADD_IMPORT = "aImport/ADD_IMPORT";

const initState = {};

export function aImportReducer(state = initState, action = {}) {
  const newState = { ...state };

  switch (action.type) {
    default:
      return newState;
  }
}
